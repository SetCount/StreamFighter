# Wails-generated bindings — regenerated by `wails generate module`.
# Keep prettier off them so it doesn't fight the generator's output
# and churn the diff every time bindings are regenerated.
wailsjs/

# Build output
dist/
